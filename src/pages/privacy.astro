---
import BaseLayout from '../layouts/BaseLayout.astro';
import PrivacyContent from '../components/legal/PrivacyContent.astro';
import { createPageI18n } from '../utils/i18n-page.ts';
import { SUPPORTED_LANGUAGES } from '@config/language-targeting';
import { generatePageHreflangTags, getPageCanonicalUrl } from '../utils/hreflang';

const i18n = await createPageI18n('en');
const currentDate = new Date().toISOString().split('T')[0];
const hreflangTags = generatePageHreflangTags('privacy', SUPPORTED_LANGUAGES);
const canonicalUrl = getPageCanonicalUrl('privacy', 'en');
---

<BaseLayout
  title={i18n.t('privacy_meta_title')}
  description={i18n.t('privacy_meta_description')}
  canonical={canonicalUrl}
  lang="en"
  hreflangTags={hreflangTags}
>
  <PrivacyContent i18n={i18n} currentDate={currentDate} lang="en" />
</BaseLayout>
